fastapi
uvicorn[standard]
pydantic
pydantic-settings
sqlalchemy
asyncpg
alembic
python-dotenv
python-multipart
PyJWT
bcrypt
email-validator
cryptography==50.0.1
jsonschema==4.26.0
# Redis is a declared future rate-limiter backend seam (app/core/rate_limit.py
# defines a Redis-backed implementation contract; the MVP ships the in-memory
# limiter). No Redis infrastructure is deployed or required at runtime.
redis
httpx
pgvector
pypdf
# Test-only: generates sample PDFs for the file-ingestion suite (import name: fpdf).
fpdf2
python-docx
beautifulsoup4
pytest
pytest-asyncio
