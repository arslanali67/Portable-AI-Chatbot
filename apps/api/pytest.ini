[pytest]
testpaths = tests
pythonpath = .
addopts = -q
asyncio_default_test_loop_scope = session
markers =
    integration: requires Docker PostgreSQL (docker compose up -d postgres)
    identity: requires Docker PostgreSQL + identity tables (alembic upgrade head)
